{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "main (debug)",
            "program": "${workspaceFolder}/main-debug",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "main (debug)"
        }
    ]
}